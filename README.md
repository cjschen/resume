# CJ Chen Resume 

LaTeX template for my personal resume

Adapted from: https://github.com/jakegut/resume

# To build on linux 

Install Latex

```
sudo apt update
sudo apt-get install texlive-latex-base
sudo apt-get install texlive-latex-extra
```

then run 

```
pdflatex -pdf resume.sty 
```